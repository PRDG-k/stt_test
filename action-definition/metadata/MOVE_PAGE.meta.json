{
  "action": "MOVE_PAGE",
  "title": "페이지 이동",
  "description": "사용자를 특정 페이지나 화면으로 이동시킨다.",
  "pages": {
    "/frontend/dashboard.html": {
      "title": "대시보드",
      "required_params": ["slId"],
      "params_config": {
        "slId": {
          "description": "발전소 식별자 (예: SL-01, SL-02)",
          "candidates": ["SL-01", "SL-02", "SL-03"]
        }
      }
    },
    "/frontend/settlement.html": {
      "title": "정산 관리",
      "required_params": ["projectId"],
      "params_config": {
        "projectId": {
          "description": "프로젝트 ID",
          "candidates": ["1", "2", "3"]
        }
      }
    },
    "/frontend/tax-report.html": {
      "title": "세무 보고서",
      "required_params": ["slId", "year"],
      "params_config": {
        "slId": {
          "description": "발전소 식별자",
          "candidates": ["SL-01", "SL-02"]
        },
        "year": {
          "description": "조회 연도 (YYYY)",
          "candidates": ["2024", "2025", "2026"]
        }
      }
    }
  },
  "llmHints": {
    "notes": [
      "url은 시스템에서 허용된 경로만 사용한다.",
      "각 URL마다 요구되는 params가 다를 수 있으므로 'pages' 설정을 참조한다.",
      "사용자가 구체적인 화면명을 말하지 않고 '보고서', '화면' 등 카테고리만 말할 경우 url을 비워두어 추가 질문을 유도한다."
    ]
  }
}